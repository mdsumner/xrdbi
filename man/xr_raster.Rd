% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{xr_raster}
\alias{xr_raster}
\title{Render a result in gdalraster's in-memory raster format}
\usage{
xr_raster(
  x,
  statement = NULL,
  as_list = FALSE,
  max_cells = getOption("xrdbi.max_cells", 1e+07),
  force = FALSE
)
}
\arguments{
\item{x}{an XarrayResult, or an XarrayConnection (with \code{statement})}

\item{statement}{a Python expression, when \code{x} is a connection}

\item{as_list}{if TRUE, a list of band vectors instead of one
band-interleaved vector}

\item{max_cells}{guard threshold, as elsewhere}

\item{force}{bypass the guard}
}
\value{
a vector (or list of band vectors) with attribute \code{gis},
interchangeable with the output of gdalraster \code{read_ds}
}
\description{
An alternate rendering: where \code{\link[DBI:dbFetch]{DBI::dbFetch()}} gives the tidy
long-form data frame, \code{xr_raster()} gives the slab in the same
in-memory form as gdalraster \code{read_ds()} a plain vector of pixel
values in left-to-right, top-to-bottom order, band-sequential, with
a \code{gis} attribute carrying \code{type}, \code{bbox}, \code{dim}, \code{srs}, and
\code{datatype}. No dataset is created and no bytes go through GDAL; the
array converts directly via numpy (no pandas hop), and gdalraster
is not needed at all.
}
\details{
The selected object must be a DataArray that squeezes to 2 or 3
dimensions: (y, x) for a single band, (band, y, x) for a stack
(e.g. a time series of slabs). The y and x dimension coordinates
must be 1-D and regular; the half-cell bbox expansion assumes
cell-centre coordinates. Ascending or descending coordinate order
is handled on either axis (rows and columns are reordered into
top-left pixel order). Curvilinear grids have no affine rendering
and error.
}

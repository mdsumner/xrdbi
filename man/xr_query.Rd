% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{xr_query}
\alias{xr_query}
\title{Compile a structured selection to a Python expression}
\usage{
xr_query(vars = NULL, sel = NULL, isel = NULL, method = NULL, tail = NULL)
}
\arguments{
\item{vars}{character vector of data variable names, NULL for all}

\item{sel}{named list of label-based selections}

\item{isel}{named list of integer (zero-based) selections}

\item{method}{optional method for inexact \code{sel} matches, e.g. "nearest"}

\item{tail}{extra Python method chain appended verbatim, e.g.
\code{".mean('time')"}}
}
\value{
character, a Python expression over \code{ds}
}
\description{
A thin, inspectable alternative to writing the Python yourself. The
return value is a plain string, so \code{dbGetQuery(con, xr_query(...))}
and \code{dbGetQuery(con, "ds.sst.isel(time=0)")} are the same thing. Print
it, log it, put it in a registry row.
}
\details{
Rules for \code{sel} and \code{isel} values:
\itemize{
\item a length-2 vector becomes a Python \code{slice(a, b)} (a range on that
dimension; for \code{sel} this is label-based and inclusive per xarray)
\item a length-1 value is a scalar selection (drops the dimension)
\item longer vectors become Python lists (fancy indexing)
}

NOTE \code{isel} uses zero-based Python indices, faithfully: this function
interpolates values into Python source and does not translate them.
The compiled expression is the contract; inspect it.
}
\examples{
xr_query(vars = "sst", sel = list(lat = c(-80, -60)), isel = list(time = 0))
# "ds[['sst']].sel(lat=slice(-80, -60)).isel(time=0)"
}

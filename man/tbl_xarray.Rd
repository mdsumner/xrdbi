% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl.R
\name{tbl_xarray}
\alias{tbl_xarray}
\alias{tbl.XarrayConnection}
\title{A lazy dplyr table over an xarray variable}
\usage{
tbl.XarrayConnection(src, from, ...)
}
\arguments{
\item{src}{an XarrayConnection}

\item{from}{a data variable name}

\item{...}{unused}
}
\description{
\code{tbl(con, name)} returns a lazy object in the dtplyr/duckplyr mould:
dplyr verbs accumulate a spec, nothing touches Python until
\code{\link[dplyr:collect]{dplyr::collect()}}. The spec compiles to a single Python statement
(inspect it with \code{\link[dplyr:show_query]{dplyr::show_query()}}), executed through
\code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}} like any other statement.
}
\details{
Predicate handling is pushdown-with-residuals:
\itemize{
\item dimension-coordinate predicates compile to an inclusive \code{.sel()}
hull, so only the chunks the slab touches are read
\item anything the hull cannot express exactly (strict inequalities,
predicates on data variables) is kept as a pandas \code{.query()}
applied Python-side, before conversion to R
\item at collect time each hull dimension is asked for its direction,
and the slice is oriented to match: \code{filter(latitude > 18, latitude < 19)} works identically on ascending and DESCENDING
coordinates
}
}

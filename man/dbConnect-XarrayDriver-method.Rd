% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/driver.R
\name{dbConnect,XarrayDriver-method}
\alias{dbConnect,XarrayDriver-method}
\title{Connect to an xarray object}
\usage{
\S4method{dbConnect}{XarrayDriver}(
  drv,
  dsn = NULL,
  ...,
  open = "open_dataset",
  builder = NULL,
  object = NULL
)
}
\arguments{
\item{drv}{result of \code{\link[=xarray]{xarray()}}}

\item{dsn}{character string openable by xarray}

\item{...}{keyword arguments passed to the xarray open function}

\item{open}{name of the xarray open function, default "open_dataset"}

\item{builder}{function taking the xarray module, returning a Dataset}

\item{object}{an existing Python xarray object to adopt}
}
\description{
Exactly one of \code{dsn}, \code{builder}, \code{object} must be supplied.
}
\details{
\itemize{
\item \code{dsn}: a string that xarray can open. Passed to \verb{xr.<open>(dsn, ...)}
where \code{open} is \code{"open_dataset"} by default (also useful:
\code{"open_zarr"}, \code{"open_mfdataset"}, \code{"open_datatree"}). Extra named
arguments in \code{...} are passed through as Python keyword arguments,
e.g. \code{engine = "rasterio"}, \code{chunks = list()}, \code{decode_times = TRUE}.
\item \code{builder}: an R function of one argument (the xarray module, not
converted) returning a Dataset. Use this when construction needs more
than a single open call, e.g. virtualizarr or icechunk session setup.
\item \code{object}: an existing reticulate Python object (an xarray Dataset),
adopted as-is. Handy when the object was built interactively with
\code{reticulate::py_run_string()} or in a repl.
}

The connection holds a reference to the Python object; nothing is
computed or converted until a result is fetched.
}
